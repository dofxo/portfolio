import { SideBarContent } from ".";
import { SideBarDrawer } from "../drawer";

const SideBar = () => {
  return (
    <>
      <SideBarDrawer />
      <SideBarContent />
    </>
  );
};

export default SideBar;
